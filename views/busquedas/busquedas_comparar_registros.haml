%h3 Registros
%ul.breadcrumb
  %li
    %a{:href=>url("/revision/#{@revision.id}")}=@revision.nombre
  %li
    %a{:href=>url("/revision/#{@revision.id}/busquedas")} Búsquedas
  %li.active
    Comparación de registro por búsquedas

%table.tablesorter
  %thead
    %th{:style=>'width:50%'} Documento
    - @busquedas_id.each do |busqueda_id|
      -busqueda=Busqueda[busqueda_id]
      %th="#{busqueda.base_bibliografica[:nombre]}<br>#{busqueda[:fecha]}"
    %th n
  %tbody
    - @cds_ordenados.each do |key,val|
      %tr
        %td=@cds_o[key].ref_apa_6_breve_html
        - @busquedas_id.each do |b_id|
          %td="#{val[:busquedas][b_id] ? 'Sí':'No'}"
        %td=val[:busquedas].length