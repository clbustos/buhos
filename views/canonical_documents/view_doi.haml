.heading
  =t_canonical_document_title(@cd[:title], t(:view_crossref_information, doi:@cd.doi), false)

%ul.breadcrumb
  %li
    %a{:href=>url("/canonical_document/#{@cd.id}")}=@cd.title
  %li.active
    =t(:view_crossref_information, doi:@cd.doi)

.well=@cr_doi.ref_apa_6_brief_html

%h3 JSON

%pre=JSON.pretty_generate(JSON.parse(@doi_json))