%p
  %strong=t(:Select_systematic_review_to_analyze)

- @rss.each do |rs|
  %h3=rs[:nombre]
  %ul.list-group
    - rs.current_stages.each do |etapa|
      %li.list-group-item
        %a{:href=>"/review/#{rs[:id]}/#{etapa}"}=t(get_stages_names[etapa])
